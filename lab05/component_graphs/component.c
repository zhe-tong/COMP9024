#include <stddef.h>

#include "lib_graph.h"
#include "graph.h"

/**
 * g_smallestComponent
 * return the *highest* index in the *smallest* component (by number of vertices)
 */
size_t g_smallestComponent(graph g)
{
    // TODO: replace this with your code
    // make the compiler happy
    (void) g;
    return 0;
}

/**
 * g_largestComponent
 * return the *lowest* index in the *largest* component (by number of edges)
 */
size_t g_largestComponent(graph g)
{
    // TODO: replace this with your code
    // make the compiler happy
    (void) g;
    return 0;
}
